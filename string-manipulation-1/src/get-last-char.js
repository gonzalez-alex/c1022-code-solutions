/* exported getLastChar */

/*
- get last index of string and store it in a variable
- return string at the last index from the function
*/

function getLastChar(string) {
  var lastIndex = string.length - 1;
  return string[lastIndex];
}
